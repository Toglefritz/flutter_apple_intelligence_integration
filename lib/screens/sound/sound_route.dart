import 'package:demo_app/screens/sound/sound_controller.dart';
import 'package:flutter/material.dart';

/// The [SoundRoute] contains a series of windows that demonstrate each of the sound features available from the
/// Apple Machine Learning framework.
class SoundRoute extends StatefulWidget {
  /// Creates a new instance of [SoundRoute].
  const SoundRoute({super.key});

  @override
  SoundController createState() => SoundController();
}
